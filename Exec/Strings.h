// Set the string file version.
// This must match the version defined in the the language file
VERSION(.9.2)

// Command names in dropdown box
STR(RUN)
STR(WEB)
STR(BROWSER)
STR(MAIL)
STR(NEWMAIL)

// Command descriptions
STR(RUN_DESC)
STR(WEB_DESC)
STR(BROWSER_DESC)
STR(MAIL_DESC)
STR(NEWMAIL_DESC)

STR(WEBSITE)

STR(ADDRESS)

STR(FILE)
STR(START)
STR(ARGS)
STR(BROWSE)
STR(RUNDESC)

STR(EXE_FILES)
STR(ALL_FILES)

STR(MAXIMIZED)
STR(MINIMIZED)
STR(NORMAL)
