// The plugin version.  This must match the version of the string files
VERSION(1.0)

// Command names in dropdown box
STR(SCRIPT)

// Command descriptions
STR(SCRIPT_DESC)

STR(IDENTIFIER)
STR(HELP)

STR(OPTIONS)

STR(ENVIRONMENT)
STR(ENV_NEW)
STR(ENV_REUSE)
STR(ENV_GLOBAL)

STR(COMMAND)

STR(ARGUMENTS)

STR(LUA_FILES)
STR(ALL_FILES)
